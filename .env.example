# Azure App Configuration Environment Template
# 
# Instructions:
# 1. Copy this file to .env in the same directory
# 2. Update the values below with your actual Azure App Configuration settings
# 3. The launch.json configurations will automatically load these environment variables

# Enable/Disable Azure App Configuration (set to true when you have valid Azure App Config setup)
AZURE_APP_CONFIG_ENABLED=true

# Required: Replace with your actual Azure App Configuration endpoint
AZURE_APP_CONFIG_ENDPOINT=

AZURE_APP_CONFIG_CONNECTION_STRING=Endpoint=


# Alternative: Use connection string instead of endpoint (comment out endpoint above if using this)
# AZURE_APP_CONFIG_CONNECTION_STRING=your-connection-string-here

# Optional: Environment label for configuration (leave empty for default)
AZURE_APP_CONFIG_LABEL=

# For production environment, uncomment and set:
# AZURE_APP_CONFIG_LABEL=prod

# Optional: Azure credentials (if using service principal authentication)
# AZURE_CLIENT_ID=your-client-id
# AZURE_CLIENT_SECRET=your-client-secret
# AZURE_TENANT_ID=your-tenant-id

# Optional: For managed identity (set to true when running in Azure)
# AZURE_USE_MANAGED_IDENTITY=false
